
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="WeSupplyCam.Archivos"
             Title="Archivos">
  

    
    
    <StackLayout Padding="10" VerticalOptions="FillAndExpand">
   
 
        <CollectionView ItemsSource="{Binding Elementos}" VerticalOptions="FillAndExpand">
            <CollectionView.ItemTemplate>
                <DataTemplate>
                    <StackLayout Orientation="Vertical" Padding="5" >
                        <Label  Margin="2" Text="{Binding Name}" />
                        <Button  Margin="2" Text="Eliminar" Clicked="Eliminar" BackgroundColor="#D34641" Grid.Column="0" />
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>


                   
                            <Button  Margin="2"  Text="Compartir" Clicked="Compartir" Grid.Column="0" />
                            <Button  Margin="2"  Text="Editar" BackgroundColor="#F37B47" Clicked="Editar" Grid.Column="1"/>

                        </Grid>

                     
                    </StackLayout>
                </DataTemplate>
            </CollectionView.ItemTemplate>
        </CollectionView>

     
    </StackLayout>



</ContentPage>